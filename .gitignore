# Große Unterrichtsmaterialien (bleiben lokal, nicht im Repo)
# Jeder Ordner, dessen Name "material" enthält, samt Inhalt, auf jeder Ebene.
# Ersetzt die frühere Einzelaufzählung der material-Ordner.
**/*[Mm]aterial*/

# Lokale Claude-Code-Berechtigungen (maschinenspezifisch)
/.claude/settings.local.json
